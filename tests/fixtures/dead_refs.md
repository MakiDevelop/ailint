# Config with Dead References

## Documentation

For deployment instructions, see `rules/nonexistent.md`.

The full architecture guide is at `~/fake/path.md`.

## Setup

Run the setup script at `./missing-file.txt` before starting.

## Valid Reference

This file exists: `tests/fixtures/valid_ref_target.txt`.
